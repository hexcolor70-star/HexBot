name: HEX Color Video Uploader

on:
  workflow_dispatch:
  repository_dispatch:
    types: [trigger_upload]

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      # 1. Быстрый чекаут (без лишней истории)
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          persist-credentials: false

# 2. Подключение локального FFmpeg из репозитория
      - name: Set up Local FFmpeg
        run: |
          chmod +x bin/ffmpeg
          echo "$PWD/bin" >> $GITHUB_PATH

      # 3. Настройка Python
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 4. Кэш для Python-библиотек (чтобы не качать каждый раз)
      - name: Cache Pip Packages
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      # 5. Установка Python-зависимостей
      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 6. Запуск твоего скрипта
      - name: Run Script
        env:
          G_TOKEN_JSON: ${{ secrets.G_TOKEN_JSON }}
        run: python main.py
        
